<% content_for :title, "SceneSync-Add New Movie" %>

<div class="container mt-4">
  <h1 class="mb-4">➕ Add a New Movie</h1>

  <div class="card p-4 shadow-sm">
    <%= render "form", movie: @movie %>
  </div>

  <div class="mt-3">
    <%= link_to "⬅️ Back to Movies", movies_path, class: "btn btn-secondary" %>
  </div>
</div>
