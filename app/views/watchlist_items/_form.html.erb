<%= form_with(model: watchlist_item) do |form| %>
  <% if watchlist_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(watchlist_item.errors.count, "error") %> prohibited this watchlist_item from being saved:</h2>

      <ul>
        <% watchlist_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :movie_id, style: "display: block" %>
    <%= form.text_field :movie_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
